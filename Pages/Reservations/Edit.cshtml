@page
@model CinemaAplicatieWEB.Pages.Reservations.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Reservation.Id" />
            <div class="form-group">
                <label asp-for="Reservation.UserId" class="control-label"></label>
                <select asp-for="Reservation.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="Reservation.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.ShowtimeId" class="control-label"></label>
                <select asp-for="Reservation.ShowtimeId" class="form-control" asp-items="ViewBag.ShowtimeId"></select>
                <span asp-validation-for="Reservation.ShowtimeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.Seats" class="control-label"></label>
                <input asp-for="Reservation.Seats" class="form-control" />
                <span asp-validation-for="Reservation.Seats" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.ReservationTime" class="control-label"></label>
                <input asp-for="Reservation.ReservationTime" class="form-control" />
                <span asp-validation-for="Reservation.ReservationTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
